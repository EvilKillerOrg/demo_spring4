IoC(Inversion of Contro) 思想是:反转资源获取的方向 容器主动将资源推送给它所管理的组件,组件要做的就是选择一种合适的方式接受资源
DI(Dependency Injection) 组件以一些预定好的方式(如setter)接受来自容器的资源注入
AOP(Aspect Oriented Programming) 面向切面编程,(切面指的是横切关注点)把关注点模块化到一个对象(切面)里
好处:1每个逻辑只位于一个位置代码不分散,便于维护 2业务模块简洁,只包含业务代码